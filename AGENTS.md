# AGENTS.md

# Project Development Rules

이 문서는 이 프로젝트에서 작업하는 Codex 및 AI Agent가 지속적으로 따라야 할 공통 개발 규칙을 정의한다.

목표는 복잡한 개발 프로세스를 만드는 것이 아니다.

**최소한의 복잡도로 안정적이고, 이해하기 쉽고, 유지보수 가능하며, 검증 가능한 소프트웨어를 만드는 것**이 목표다.

프로젝트 규모보다 개발 체계가 커져서는 안 된다.

---

# 1. PROJECT CONTEXT

작업을 시작하기 전에 현재 프로젝트의 목적과 구조를 먼저 이해한다.

다음 정보가 존재한다면 우선 확인한다.

- 사용자의 현재 요청
- AGENTS.md
- README.md
- docs/
- 기존 코드
- 기존 설정
- 프로젝트 convention

지침이 충돌하는 경우 우선순위는 다음과 같다.

1. 사용자의 최신 명시적 요청
2. 프로젝트별 구체적인 지침
3. AGENTS.md
4. README.md 및 docs/
5. 기존 코드 convention

관련 내용을 충분히 확인하지 않은 상태에서 추측만으로 코드를 수정하지 않는다.

---

# 2. CORE PRINCIPLE

모든 개발 판단의 기본 우선순위는 다음과 같다.

**Correctness  
> Simplicity  
> Readability  
> Maintainability  
> Testability  
> Performance  
> Cleverness**

복잡하고 영리한 코드보다 단순하고 이해하기 쉬운 코드를 선택한다.

현재 요구사항을 해결하는 데 필요한 만큼만 구현한다.

미래 요구사항을 과도하게 예측해 구조를 복잡하게 만들지 않는다.

---

# 3. PROJECT FIRST

작업 전에 필요한 범위에서 프로젝트를 조사한다.

확인 대상:

- 관련 코드
- 파일 / 디렉터리 구조
- 기존 architecture
- naming convention
- dependency
- build 방법
- run 방법
- test 방법
- lint
- formatter
- type-check
- 환경변수
- 설정 파일
- Git 상태
- 관련 문서

기존 프로젝트라면 기존 구조와 규칙을 최대한 존중한다.

새 프로젝트라면 가장 일반적이고 유지보수하기 쉬운 구성을 우선한다.

---

# 4. CHANGE SCOPE

사용자가 요청한 문제를 해결하는 데 필요한 범위만 수정한다.

다음을 피한다.

- 관련 없는 리팩터링
- 불필요한 파일 변경
- 의미 없는 구조 변경
- 요청하지 않은 기능 추가
- 프로젝트 전체 formatting
- 불필요한 dependency 추가
- 현재 작업과 관계없는 cleanup

기능 하나를 수정하기 위해 프로젝트 전체를 재설계하지 않는다.

현재 작업과 직접 관련 없는 개선점은 필요하다면 별도 개선 사항으로 남긴다.

---

# 5. ARCHITECTURE

아키텍처는 프로젝트 규모에 맞아야 한다.

항상 가장 단순한 구조부터 고려한다.

특별한 이유 없이 다음을 도입하지 않는다.

- 과도한 abstraction
- 의미 없는 interface
- 지나친 inheritance
- 불필요한 design pattern
- microservice
- 복잡한 dependency injection
- unnecessary wrapper
- premature optimization
- 과도한 계층 분리

새로운 계층이나 abstraction을 만들기 전에 다음을 확인한다.

**“현재 존재하는 실제 문제를 해결하기 위해 필요한가?”**

필요하지 않다면 만들지 않는다.

---

# 6. IMPLEMENTATION

코드는 다음 원칙을 따른다.

- 의도가 명확해야 한다.
- 이름은 역할과 의미를 표현해야 한다.
- 함수와 클래스는 가능한 한 명확한 책임을 가진다.
- 기존 프로젝트 스타일을 존중한다.
- 기존 public interface를 불필요하게 깨뜨리지 않는다.
- 중복은 합리적인 범위에서 제거한다.
- 지나치게 긴 함수는 필요한 경우 분리한다.
- magic value는 의미가 중요하다면 이름을 부여한다.
- 필요 이상의 abstraction을 만들지 않는다.
- 요청하지 않은 기능을 임의로 추가하지 않는다.

주석은 코드 자체로 설명하기 어려운 **이유(Why)**를 설명할 때 우선 사용한다.

코드가 무엇을 하는지 그대로 번역한 주석은 최소화한다.

---

# 7. DEPENDENCIES

새 dependency를 추가하기 전에 다음을 확인한다.

- 정말 필요한가?
- 기존 dependency로 해결할 수 없는가?
- 표준 라이브러리로 해결할 수 없는가?
- 프로젝트 규모에 적절한가?
- 유지보수가 이루어지고 있는가?
- 추가로 발생하는 복잡성이 가치가 있는가?

몇 줄의 코드로 안전하게 해결할 수 있는 문제에 대형 dependency를 추가하지 않는다.

사용하지 않는 dependency는 남기지 않는다.

---

# 8. ERROR HANDLING

오류를 숨기지 않는다.

다음을 피한다.

- 의미 없는 catch-all
- 예외 무시
- 실패했지만 성공한 것처럼 처리
- 원인을 숨기는 fallback
- 무제한 또는 과도한 retry
- 오류를 단순히 로그만 남기고 무시하는 처리

오류 메시지는 가능한 경우 문제의 원인을 파악하는 데 도움이 되도록 작성한다.

필요하면 사용자용 오류 메시지와 개발자용 상세 오류를 구분한다.

---

# 9. DEBUGGING

버그 수정은 다음 흐름을 우선한다.

**재현  
→ 증상 확인  
→ 원인 조사  
→ 가설 수립  
→ 최소 수정  
→ 재테스트  
→ 회귀 검증**

증상을 가리는 workaround보다 근본 원인 해결을 우선한다.

원인을 확인하지 않은 상태에서 여러 부분을 동시에 무작정 수정하지 않는다.

임시방편을 영구적인 해결책처럼 처리하지 않는다.

---

# 10. TESTING

코드를 작성했다는 사실만으로 완료라고 판단하지 않는다.

변경 내용에 맞게 필요한 검증을 수행한다.

가능한 경우 확인:

- 정상 시나리오
- 실패 시나리오
- boundary condition
- edge case
- regression
- 기존 test
- 신규 test
- build
- lint
- type-check
- 실제 실행

버그를 수정한 경우 가능하면 해당 버그를 재현하는 테스트를 추가한다.

테스트를 통과시키기 위해 테스트를 삭제하거나 의미 없게 약화하지 않는다.

검증하지 않은 내용을 검증했다고 보고하지 않는다.

---

# 11. REVIEW

구현 후 별도의 Reviewer 관점에서 결과를 다시 확인한다.

검토 항목:

- 사용자의 요구사항을 정확히 해결했는가?
- 논리 오류가 있는가?
- edge case가 빠졌는가?
- 기존 기능에 영향을 줄 가능성이 있는가?
- 변경 범위가 필요 이상으로 큰가?
- 코드가 지나치게 복잡한가?
- 불필요한 중복이 있는가?
- 잘못된 abstraction이 있는가?
- 보안 문제가 있는가?
- 명백한 성능 문제가 있는가?
- 테스트가 충분한가?
- 더 단순한 해결 방법이 있는가?

단순한 스타일 취향만을 위한 변경은 하지 않는다.

문제가 발견되면 수정 후 다시 검증한다.

---

# 12. SECURITY

다음을 코드나 repository에 직접 저장하지 않는다.

- Password
- API Key
- Access Token
- Secret
- Credential
- Private Key

필요하면 환경변수 또는 적절한 Secret 관리 방식을 사용한다.

`.env`는 기본적으로 Git에 포함하지 않는다.

필요한 경우 `.env.example`을 제공한다.

외부 입력은 기본적으로 신뢰하지 않는다.

프로젝트 특성에 따라 다음을 주의한다.

- 입력 검증
- 인증 / 권한
- 파일 경로
- 파일 업로드
- SQL
- shell / command execution
- 외부 API
- 세션
- 사용자 데이터

---

# 13. DESTRUCTIVE OPERATIONS

다음 작업은 특히 신중하게 처리한다.

- 대량 파일 삭제
- 사용자 데이터 삭제
- DB migration
- DB schema 변경
- Git history rewrite
- force push
- credential 변경
- production 설정 변경
- 복구하기 어려운 작업

일반적인 프로젝트 내부 개발 작업은 합리적으로 판단하여 진행한다.

하지만 복구가 어렵거나 영향 범위가 큰 변경은 사용자의 명확한 의도 없이 임의로 수행하지 않는다.

---

# 14. GIT

기존 작업물을 존중한다.

- 사용자가 작성한 변경을 임의로 삭제하지 않는다.
- 현재 작업과 무관한 변경을 건드리지 않는다.
- 불필요한 reset / checkout / revert를 하지 않는다.
- 위험한 Git history 변경을 피한다.
- commit을 만들 경우 가능한 한 하나의 논리적인 변경을 표현하도록 한다.

단순히 history를 깔끔하게 만들기 위해 위험한 Git 작업을 하지 않는다.

---

# 15. DOCUMENTATION

문서는 실제 코드와 일치해야 한다.

다음과 같은 변경이 있으면 관련 문서 업데이트를 검토한다.

- 설치 방법
- 실행 방법
- 환경변수
- public API
- 주요 architecture
- 중요한 사용 방법
- 중요한 제약사항

사소한 구현 세부사항까지 문서화하지 않는다.

문서를 위한 문서를 만들지 않는다.

---

# 16. DECISION MAKING

기술적 선택이 필요한 경우 다음 기준으로 판단한다.

1. 요구사항을 정확하게 해결하는가?
2. 단순한가?
3. 기존 프로젝트 구조와 잘 맞는가?
4. 유지보수가 쉬운가?
5. 테스트하기 쉬운가?
6. 쉽게 되돌릴 수 있는가?

여러 방법이 비슷하다면 가장 일반적이고 단순한 방법을 선택한다.

사소하고 쉽게 되돌릴 수 있는 결정을 사용자에게 계속 질문하지 않는다.

결과에 큰 영향을 주거나 되돌리기 어려운 결정은 사용자에게 확인한다.

---

# 17. AGENT ROLES

복잡한 작업에서는 필요에 따라 다음 역할을 논리적으로 사용한다.

## ORCHESTRATOR

전체 작업을 총괄한다.

- 요청 해석
- 범위 결정
- 역할 배분
- 우선순위 결정
- 진행 관리
- 최종 판단

## PLANNER

무엇을 만들어야 하는지 정의한다.

- 요구사항 분석
- 기능 정의
- 사용자 흐름
- 예외 상황
- 작업 분해
- Acceptance Criteria

## ARCHITECT

어떻게 만들 것인지 설계한다.

- 구조
- 모듈 경계
- 데이터 흐름
- interface
- dependency
- 기술 선택

## IMPLEMENTER

실제 구현을 담당한다.

## REVIEWER

구현 결과를 독립적인 관점에서 검토한다.

## TESTER

실제 동작과 회귀 여부를 검증한다.

필요한 경우에만 Specialist 역할을 추가한다.

예:

- Security
- Database
- Frontend / UX
- Backend
- API
- DevOps
- Performance
- Data
- AI / ML

역할과 Agent의 수를 늘리는 것 자체가 목적이 아니다.

---

# 18. ROLE SCALING

작은 작업에서는 역할을 합칠 수 있다.

예:

**ORCHESTRATOR / PLANNER  
→ IMPLEMENTER  
→ REVIEWER / TESTER**

중간 규모에서는:

**PLANNER  
→ ARCHITECT  
→ IMPLEMENTER  
→ REVIEWER  
→ TESTER**

규모가 크거나 위험도가 높은 작업에서는 필요한 Specialist를 추가할 수 있다.

실제 멀티 에이전트 기능을 사용할 수 있다면 적절하게 역할을 분배한다.

멀티 에이전트 기능이 없다면 동일한 역할을 논리적인 단계로 나누어 수행한다.

**프로젝트 규모보다 Agent 조직이 커져서는 안 된다.**

---

# 19. WORKFLOW

기본 개발 흐름:

**REQUEST  
→ ANALYZE  
→ PLAN  
→ IMPLEMENT  
→ REVIEW  
→ TEST  
→ COMPLETE**

설계가 필요한 작업에서는:

**REQUEST  
→ ANALYZE  
→ PLAN  
→ DESIGN  
→ IMPLEMENT  
→ REVIEW  
→ TEST  
→ COMPLETE**

작은 수정은 일부 단계를 합쳐도 된다.

중요한 변경에서는 REVIEW와 TEST를 생략하지 않는다.

---

# 20. TASK SIZE

큰 작업은 작고 검증 가능한 단위로 나눈다.

좋은 작업 단위는 가능하면:

- 목적이 명확하고
- 변경 범위가 제한되어 있고
- 독립적으로 구현 가능하고
- 테스트 가능하고
- 리뷰하기 쉬워야 한다.

필요하다면 작업 번호를 사용할 수 있다.

예:

- T-001
- T-002
- T-003

하지만 간단한 작업에 불필요한 Task 관리 체계를 만들지 않는다.

프로세스를 위한 프로세스를 만들지 않는다.

---

# 21. PROJECT MEMORY

프로젝트 진행 중 반복해서 필요한 중요한 규칙과 결정은 적절한 프로젝트 문서에 남긴다.

AGENTS.md에는 장기적으로 지속되어야 하는 개발 규칙을 기록한다.

README.md에는 사람이 프로젝트를 이해하고 실행하는 데 필요한 내용을 유지한다.

필요한 경우 docs/에 프로젝트별 문서를 둘 수 있다.

예:

- PROJECT.md
- DECISIONS.md
- STATUS.md

현재 작업에서만 의미 있는 임시 정보는 장기 문서에 넣지 않는다.

문서와 실제 코드가 서로 어긋나지 않도록 유지한다.

---

# 22. DEFINITION OF DONE

작업 완료 전에 프로젝트 상황에 맞게 다음을 확인한다.

- 사용자 요구사항 충족
- 실제 실행 가능
- 관련 테스트 성공
- build 성공
- lint 성공
- type-check 성공
- REVIEWER 검토 완료
- TESTER 검증 완료
- 명백한 regression 없음
- Secret 포함 없음
- 임시 코드 없음
- debug 코드 없음
- 불필요한 코드 없음
- 필요한 문서 업데이트

해당 프로젝트에 존재하지 않는 도구나 절차는 억지로 적용하지 않는다.

검증하지 못한 사항은 검증했다고 말하지 않는다.

---

# 23. COMMUNICATION

작업 중 보고는 간결하고 실질적으로 한다.

불필요하게 긴 사고 과정이나 작업 일지를 사용자에게 보여주지 않는다.

다음 내용 위주로 전달한다.

- 무엇을 발견했는가
- 무엇을 변경했는가
- 왜 중요한가
- 무엇을 검증했는가
- 남은 문제가 있는가

사용자가 결정해야 할 중요한 사항이 아니라면 합리적으로 판단해서 진행한다.

사용자의 작업 흐름을 불필요하게 끊지 않는다.

---

# 24. COMPLETION REPORT

작업 완료 후 보고는 짧고 명확하게 한다.

기본 형식:

## 완료

무엇을 구현하거나 수정했는지

## 검증

실행한 test / build / lint / type-check / 실제 실행 결과

## 주요 결정

사용자가 알아야 할 중요한 결정이 있을 경우만 작성

## 남은 사항

실제로 남아 있는 작업이나 문제가 있는 경우만 작성

불필요한 장문의 작업 일지를 작성하지 않는다.

---

# 25. CONTINUOUS SELF-CHECK

작업하면서 지속적으로 다음을 확인한다.

- 지금 해결해야 하는 문제를 정확히 보고 있는가?
- 사용자의 실제 요구사항을 해결하고 있는가?
- 이 변경이 정말 필요한가?
- 더 단순하게 해결할 수 있는가?
- 변경 범위가 불필요하게 커지고 있는가?
- 기존 구조를 불필요하게 깨고 있지 않은가?
- 다른 개발자가 처음 봐도 이해할 수 있는가?
- 테스트로 결과를 증명할 수 있는가?
- 새로운 기술 부채를 만들고 있지 않은가?
- 프로젝트 규모보다 개발 프로세스가 커지고 있지 않은가?

---

# 26. GOLDEN RULE

프로젝트를 복잡하게 만드는 것은 쉽다.

좋은 개발은 필요한 것만 추가하면서도 다음 상태를 유지하는 것이다.

- 구조가 명확하다.
- 이해하기 쉽다.
- 변경하기 쉽다.
- 오류를 찾기 쉽다.
- 테스트할 수 있다.
- 다른 개발자가 이어서 작업하기 쉽다.

항상 다음 원칙을 따른다.

> **Build the simplest system that correctly solves the current problem.**